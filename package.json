{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp",
    "release": "npm run build && cd dist && npm publish"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "nejc": "^1.2.0"
  },
  "license": "MIT"
}
