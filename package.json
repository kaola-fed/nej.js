{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp",
    "release": "npm run build && cd nej-commonjs && npm publish"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-insert": "^0.5.0",
    "nejc": "^1.2.0"
  },
  "license": "MIT"
}
